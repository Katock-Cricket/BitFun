# Vendored @foreshadow/core (historical)

This directory is a **historical snapshot** kept for reference only.

**Runtime dependency is npm:**

```json
"@foreshadow/core": "^0.2.0"
```

in `src/web-ui/package.json`. Do **not** point package.json or Vite aliases at this tree.

- Upstream package: https://www.npmjs.com/package/@foreshadow/core
- Upstream repo: independent foreshadow Git (`packages/core`)
- When updating BitFun: bump the semver range and refresh the lockfile; do not vendor-edit product behavior here.